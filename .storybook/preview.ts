import type { Preview } from "@storybook/react";
// Import your global CSS file to apply Tailwind styles to all stories
import '../src/index.css';

const preview: Preview = {
  parameters: {
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/i,
      },
    },
  },
};

export default preview;
